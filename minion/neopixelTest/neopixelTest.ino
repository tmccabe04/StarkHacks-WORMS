#include "src/minion.h"

void setup() {
  // none of that
}

void loop() {
  // r
  setRGB(64, 0, 0);
  delay(1000);

  // g
  setRGB(0, 64, 0);
  delay(1000);

  // b
  setRGB(0, 0, 64);
  delay(1000);
}
